# bayou — web UI

The front-end for [bayou](https://github.com/epsteinj/bayou-desktop): a private,
local AI agent. This page is just the UI — the model and your chats run on your
own Mac via the **bayou helper** (it connects to `ws://localhost:8780`).

No helper yet? The page shows install instructions. Nothing leaves your machine.
